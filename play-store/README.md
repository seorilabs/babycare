# Google Play Metadata

Google Play Console 등록과 release metadata를 둔다.

실제 앱에서는 `google-play.config.example.json`을 `google-play.config.json`으로 복사해 확정값만 채운다.

`data-safety-responses.json`은 앱의 Data Safety 답변 원장이다. 최신 Play Console CSV 템플릿에 `scripts/apply-google-play-data-safety.py`로 반영한다.
